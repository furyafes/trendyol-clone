<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Trendyol</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <!-- Header -->
    <header class="navbar navbar-expand-lg navbar-light bg-white border-bottom sticky-top">
        <div class="container">
            <!-- Logo -->
            <a class="navbar-brand fw-bold text-danger" href="/">
                <i class="fas fa-shopping-bag me-2"></i>Trendyol
            </a>

            <!-- Search Bar -->
            <div class="flex-grow-1 mx-4">
                <form class="d-flex" action="/search" method="GET">
                    <div class="input-group">
                        <input class="form-control" type="search" name="q" placeholder="Aradığınız ürün, kategori veya markayı yazınız" 
                               value="<%= locals.query || '' %>" aria-label="Search">
                        <button class="btn btn-outline-danger" type="submit">
                            <i class="fas fa-search"></i>
                        </button>
                    </div>
                </form>
            </div>

            <!-- Navigation -->
            <nav class="navbar-nav ms-auto">
                <div class="d-flex align-items-center">
                    <!-- Cart -->
                    <a class="nav-link position-relative me-3" href="/cart">
                        <i class="fas fa-shopping-cart fs-5"></i>
                        <% if (locals.cart && cart.length > 0) { %>
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                                <%= cart.length %>
                            </span>
                        <% } %>
                    </a>

                    <!-- User Menu -->
                    <% if (locals.user) { %>
                        <div class="dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="fas fa-user me-1"></i>
                                <%= user.firstName %>
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/account">Hesabım</a></li>
                                <li><a class="dropdown-item" href="/account/orders">Siparişlerim</a></li>
                                <li><a class="dropdown-item" href="/account/profile">Profil</a></li>
                                <% if (user.isAdmin) { %>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item text-primary" href="/admin">
                                        <i class="fas fa-user-shield me-1"></i>Admin Panel
                                    </a></li>
                                <% } %>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/auth/logout">Çıkış Yap</a></li>
                            </ul>
                        </div>
                    <% } else { %>
                        <a class="nav-link" href="/auth/login">Giriş Yap</a>
                        <a class="nav-link" href="/auth/register">Kayıt Ol</a>
                    <% } %>
                </div>
            </nav>
        </div>
    </header>

    <!-- Categories Bar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
        <div class="container">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/products">Tüm Ürünler</a>
                </li>
                <% if (locals.categories) { %>
                    <% categories.slice(0, 8).forEach(category => { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/products?category=<%= encodeURIComponent(category) %>">
                                <%= category %>
                            </a>
                        </li>
                    <% }); %>
                <% } %>
            </ul>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container my-4">
        <%- body %>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-light py-5 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-3">
                    <h5>Trendyol</h5>
                    <p class="text-muted">Alışverişin adresi</p>
                    <div class="social-links">
                        <a href="#" class="text-light me-2"><i class="fab fa-facebook"></i></a>
                        <a href="#" class="text-light me-2"><i class="fab fa-twitter"></i></a>
                        <a href="#" class="text-light me-2"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="text-light"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
                <div class="col-md-3">
                    <h6>Kurumsal</h6>
                    <ul class="list-unstyled">
                        <li><a href="#" class="text-muted">Hakkımızda</a></li>
                        <li><a href="#" class="text-muted">Kariyer</a></li>
                        <li><a href="#" class="text-muted">İletişim</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h6>Yardım</h6>
                    <ul class="list-unstyled">
                        <li><a href="#" class="text-muted">Sıkça Sorulan Sorular</a></li>
                        <li><a href="#" class="text-muted">Kargo Takip</a></li>
                        <li><a href="#" class="text-muted">İade ve Değişim</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h6>Müşteri Hizmetleri</h6>
                    <p class="text-muted">7/24 destek</p>
                    <p class="text-muted">0850 123 4567</p>
                </div>
            </div>
            <hr class="my-4">
            <div class="row">
                <div class="col-md-6">
                    <p class="text-muted mb-0">&copy; 2024 Trendyol Clone. Tüm hakları saklıdır.</p>
                </div>
                <div class="col-md-6 text-end">
                    <a href="#" class="text-muted me-3">Gizlilik Politikası</a>
                    <a href="#" class="text-muted">Kullanım Şartları</a>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html> 